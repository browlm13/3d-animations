# Distributions

All randomness flows through seeded generators to keep outputs reproducible.
The first recipe samples cluster radii from a normal distribution to create a
soft edge around the galactic disk while maintaining a stable mean radius.
